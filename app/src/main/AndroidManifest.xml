<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.team09app.team09app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".ViewAllItems" />
        <activity android:name=".AddNewItem" />
        <activity android:name=".Export" />
        <activity android:name=".BrowseByCategory" />
        <activity android:name=".BrowseByRoom" />
        <activity android:name=".UpdateItemActivity" />
        <activity android:name=".ViewSingleActivity"></activity>
    </application>

</manifest>